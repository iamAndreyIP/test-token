extends ../templates/main-templ.pug

block title
    title login page

block style 
    //- link(rel="stylesheet", href="src/pages/login.scss")

block content
    form.login
        div 
            input(type="text", placeholder="login", id="login")
            label(for="login") test
        input(type="password", placeholder="password")
        button.btn(href="src/pages/chats.pug") enter
        a.login-link(href="src/pages/reg.pug") go create account